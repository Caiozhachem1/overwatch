# Lazarus Overwatch link

Permanent entry point: https://caiozhachem1.github.io/overwatch/

Forwards to the current server address in `url.json`, which the Overwatch server updates itself. The app is behind sign-in; this repo holds no data.
